<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.blizzard.com/wow/ui/
..\FrameXML\UI.xsd">

	<GameTooltip name="LinkeratorTip" inherits="GameTooltipTemplate" parent="UIParent" hidden="true">
		<Scripts>
			<OnLoad>
				self:SetOwner(UIParent, "ANCHOR_NONE");
			</OnLoad>
			<OnTooltipSetItem>
				if (self.printHyperlinkID) then
					local name, link = self:GetItem();
					if (name ~= "" and link) then
						local _, _, itemID = string.find(link, "item:(%d+)");
						if (itemID) then
							GFWUtils.Print("Item ID "..itemID..": "..link);
						end
						self.printHyperlinkID = nil;
					end
				end
			</OnTooltipSetItem>
			<OnUpdate>
				if (self.printHyperlinkID) then
					local name, link = self:GetItem();
					if (name ~= "" and link) then
						local _, _, itemID = string.find(link, "item:(%d+)");
						if (itemID) then
							GFWUtils.Print("Item ID "..itemID..": "..link);
						end
						self.printHyperlinkID = nil;
					end
				end
			</OnUpdate>
		</Scripts>
	</GameTooltip>

	<Frame name="LinkeratorFrame">
		<Scripts>
			<OnLoad function="FLT_OnLoad"/>
			<OnUpdate function="FLT_OnUpdate"/>
			<OnEvent function="FLT_OnEvent"/>
		</Scripts>
	</Frame>

</Ui>
